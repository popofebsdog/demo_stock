name: Send daily Taiwan stock email

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Email sending is disabled
        run: echo "Daily email sending is temporarily disabled."
