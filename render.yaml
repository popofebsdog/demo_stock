services:
  - type: web
    name: demo-stock-screener
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python demo_server.py
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
